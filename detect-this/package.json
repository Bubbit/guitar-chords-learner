{
  "scripts": {
    "start:build": "cd dist && es-dev-server --open",
    "build": "rimraf dist && rollup -c rollup.config.js",
    "start": "es-dev-server --app-index index.html --node-resolve --open --watch",
    "start:compatibility": "es-dev-server --app-index index.html --compatibility all --node-resolve --open --watch"
  },
  "devDependencies": {
    "@joseph184/rollup-plugin-node-builtins": "^2.1.4",
    "@open-wc/building-rollup": "^0.9.0",
    "es-dev-server": "^1.5.0",
    "rimraf": "^2.6.3",
    "rollup": "^1.15.4"
  },
  "name": "detect-this",
  "version": "0.0.0",
  "description": "Webcomponent detect-this following open-wc recommendations",
  "author": "detect-this",
  "license": "MIT",
  "dependencies": {
    "@tensorflow-models/speech-commands": "^0.4.0",
    "@tensorflow/tfjs": "^1.3.1",
    "core-js": "^3.4.1",
    "lit-element": "^2.0.1",
    "lit-html": "^1.0.0"
  }
}
